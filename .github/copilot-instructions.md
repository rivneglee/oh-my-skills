# GitHub Copilot Instructions

This repository is a cross-agent skill library currently focused on log validation. Before validating logs, traces, command output, CI output, or incident evidence, inspect `skills/` for a matching skill and follow its `SKILL.md`.

Use `npm test` to validate repository structure and skill metadata.
