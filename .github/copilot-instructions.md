# GitHub Copilot Instructions

This repository is a cross-agent skill library. Before planning, documenting, reviewing, debugging, or changing code, inspect `skills/` for a matching skill and follow its `SKILL.md`.

Use `npm test` to validate repository structure and skill metadata.
